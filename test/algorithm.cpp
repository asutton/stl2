
#include <std/algorithm.hpp>

#include <cassert>
#include <forward_list>
#include <list>
#include <vector>
#include <string>


bool
is_pos(int n) { return n > 0; }

bool
is_odd(int n) { return n % 2; }


int main()
{
  // std::vector<int> v1 {1, 2, 3, 4, 5};
  // assert(stl::all_of(v1.begin(), v1.end(), is_pos));
}
